// render.cpp

#include "math.h"
#include "list.h"
#include "render.h"

void	Rrenderer::renderScene()
{

/*	int	x,y;
	RrenderContext	projection_screen;

	projection_screen.P.z = 0.0;
	projection_screen.I.x = projection_screen.I.y = 0.0;
	projection_screen.I.z = 500.0;

	for(y = 0; y < pixel_size_y; y++)
	{
		projection_screen.P.y = bufferToRendererY(y);

		for(x = 0; x < pixel_size_x; x++)
		{
			projection_screen.P.x = bufferToRendererX(x);
			//putPixel(Trace(projection_screen), x, y);
		}
	}
*/
}

/*

Ccolor	Rrenderer::Trace(RrenderContext context&)
{
	// for every object
	{
		// if ray hits object's bouding box
		{
			// for every object's polygons
			{
				// if ray hits polygon
				{
					// if intersection is clother to camera than previous one
					{
						// call shader
					}

				}

			}
		}
	}
}

  */




